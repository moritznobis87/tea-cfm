# Wichtig: numpy und pyarrow sind bewusst EXPLIZIT und exakt gepinnt.
# Neuere pyarrow/numpy-Kombinationen (z.B. pyarrow>=20 mit numpy>=2.x)
# haben in Tests zu Segmentation Faults beim Rendern von st.dataframe()
# gefuehrt (binaeres ABI-Mismatch zwischen pyarrow und numpy). Diese
# Kombination ist gegen den echten App-Ablauf (alle Tabs, Formulare,
# Toggles) getestet und stabil. Bitte NICHT auf "numpy>=..." o.ae. lockern,
# ohne erneut die komplette App zu testen.
pydantic==2.13.4
pandas==3.0.3
numpy==2.5.1
pyarrow==25.0.0
numpy-financial==1.0.0
scipy==1.18.0
pyyaml==6.0.3
streamlit==1.59.1
plotly==6.9.0
